// Copyright (c) 2004-present Facebook All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/facebookincubator/symphony/pkg/ent/kqi"
	"github.com/facebookincubator/symphony/pkg/ent/temporalfrecuency"
)

// TemporalFrecuencyCreate is the builder for creating a TemporalFrecuency entity.
type TemporalFrecuencyCreate struct {
	config
	mutation *TemporalFrecuencyMutation
	hooks    []Hook
}

// SetCreateTime sets the create_time field.
func (tfc *TemporalFrecuencyCreate) SetCreateTime(t time.Time) *TemporalFrecuencyCreate {
	tfc.mutation.SetCreateTime(t)
	return tfc
}

// SetNillableCreateTime sets the create_time field if the given value is not nil.
func (tfc *TemporalFrecuencyCreate) SetNillableCreateTime(t *time.Time) *TemporalFrecuencyCreate {
	if t != nil {
		tfc.SetCreateTime(*t)
	}
	return tfc
}

// SetUpdateTime sets the update_time field.
func (tfc *TemporalFrecuencyCreate) SetUpdateTime(t time.Time) *TemporalFrecuencyCreate {
	tfc.mutation.SetUpdateTime(t)
	return tfc
}

// SetNillableUpdateTime sets the update_time field if the given value is not nil.
func (tfc *TemporalFrecuencyCreate) SetNillableUpdateTime(t *time.Time) *TemporalFrecuencyCreate {
	if t != nil {
		tfc.SetUpdateTime(*t)
	}
	return tfc
}

// SetName sets the name field.
func (tfc *TemporalFrecuencyCreate) SetName(s string) *TemporalFrecuencyCreate {
	tfc.mutation.SetName(s)
	return tfc
}

// AddTemporalFrecuencyFkIDs adds the temporalFrecuencyFk edge to Kqi by ids.
func (tfc *TemporalFrecuencyCreate) AddTemporalFrecuencyFkIDs(ids ...int) *TemporalFrecuencyCreate {
	tfc.mutation.AddTemporalFrecuencyFkIDs(ids...)
	return tfc
}

// AddTemporalFrecuencyFk adds the temporalFrecuencyFk edges to Kqi.
func (tfc *TemporalFrecuencyCreate) AddTemporalFrecuencyFk(k ...*Kqi) *TemporalFrecuencyCreate {
	ids := make([]int, len(k))
	for i := range k {
		ids[i] = k[i].ID
	}
	return tfc.AddTemporalFrecuencyFkIDs(ids...)
}

// Mutation returns the TemporalFrecuencyMutation object of the builder.
func (tfc *TemporalFrecuencyCreate) Mutation() *TemporalFrecuencyMutation {
	return tfc.mutation
}

// Save creates the TemporalFrecuency in the database.
func (tfc *TemporalFrecuencyCreate) Save(ctx context.Context) (*TemporalFrecuency, error) {
	var (
		err  error
		node *TemporalFrecuency
	)
	tfc.defaults()
	if len(tfc.hooks) == 0 {
		if err = tfc.check(); err != nil {
			return nil, err
		}
		node, err = tfc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*TemporalFrecuencyMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = tfc.check(); err != nil {
				return nil, err
			}
			tfc.mutation = mutation
			node, err = tfc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(tfc.hooks) - 1; i >= 0; i-- {
			mut = tfc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, tfc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (tfc *TemporalFrecuencyCreate) SaveX(ctx context.Context) *TemporalFrecuency {
	v, err := tfc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (tfc *TemporalFrecuencyCreate) defaults() {
	if _, ok := tfc.mutation.CreateTime(); !ok {
		v := temporalfrecuency.DefaultCreateTime()
		tfc.mutation.SetCreateTime(v)
	}
	if _, ok := tfc.mutation.UpdateTime(); !ok {
		v := temporalfrecuency.DefaultUpdateTime()
		tfc.mutation.SetUpdateTime(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (tfc *TemporalFrecuencyCreate) check() error {
	if _, ok := tfc.mutation.CreateTime(); !ok {
		return &ValidationError{Name: "create_time", err: errors.New("ent: missing required field \"create_time\"")}
	}
	if _, ok := tfc.mutation.UpdateTime(); !ok {
		return &ValidationError{Name: "update_time", err: errors.New("ent: missing required field \"update_time\"")}
	}
	if _, ok := tfc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := tfc.mutation.Name(); ok {
		if err := temporalfrecuency.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (tfc *TemporalFrecuencyCreate) sqlSave(ctx context.Context) (*TemporalFrecuency, error) {
	_node, _spec := tfc.createSpec()
	if err := sqlgraph.CreateNode(ctx, tfc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (tfc *TemporalFrecuencyCreate) createSpec() (*TemporalFrecuency, *sqlgraph.CreateSpec) {
	var (
		_node = &TemporalFrecuency{config: tfc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: temporalfrecuency.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: temporalfrecuency.FieldID,
			},
		}
	)
	if value, ok := tfc.mutation.CreateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: temporalfrecuency.FieldCreateTime,
		})
		_node.CreateTime = value
	}
	if value, ok := tfc.mutation.UpdateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: temporalfrecuency.FieldUpdateTime,
		})
		_node.UpdateTime = value
	}
	if value, ok := tfc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: temporalfrecuency.FieldName,
		})
		_node.Name = value
	}
	if nodes := tfc.mutation.TemporalFrecuencyFkIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   temporalfrecuency.TemporalFrecuencyFkTable,
			Columns: []string{temporalfrecuency.TemporalFrecuencyFkColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: kqi.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// TemporalFrecuencyCreateBulk is the builder for creating a bulk of TemporalFrecuency entities.
type TemporalFrecuencyCreateBulk struct {
	config
	builders []*TemporalFrecuencyCreate
}

// Save creates the TemporalFrecuency entities in the database.
func (tfcb *TemporalFrecuencyCreateBulk) Save(ctx context.Context) ([]*TemporalFrecuency, error) {
	specs := make([]*sqlgraph.CreateSpec, len(tfcb.builders))
	nodes := make([]*TemporalFrecuency, len(tfcb.builders))
	mutators := make([]Mutator, len(tfcb.builders))
	for i := range tfcb.builders {
		func(i int, root context.Context) {
			builder := tfcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*TemporalFrecuencyMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, tfcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, tfcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, tfcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (tfcb *TemporalFrecuencyCreateBulk) SaveX(ctx context.Context) []*TemporalFrecuency {
	v, err := tfcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
