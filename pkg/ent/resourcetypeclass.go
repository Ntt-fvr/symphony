// Copyright (c) 2004-present Facebook All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"github.com/facebook/ent/dialect/sql"
	"github.com/facebookincubator/symphony/pkg/ent/resourcetypeclass"
)

// ResourceTypeClass is the model entity for the ResourceTypeClass schema.
type ResourceTypeClass struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ResourceTypeClassQuery when eager-loading is set.
	Edges ResourceTypeClassEdges `json:"edges"`
}

// ResourceTypeClassEdges holds the relations/edges for other nodes in the graph.
type ResourceTypeClassEdges struct {
	// ResourceTypeFk holds the value of the resource_type_fk edge.
	ResourceTypeFk []*ResourceType
	// Policies holds the value of the policies edge.
	Policies []*PermissionsPolicy
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ResourceTypeFkOrErr returns the ResourceTypeFk value or an error if the edge
// was not loaded in eager-loading.
func (e ResourceTypeClassEdges) ResourceTypeFkOrErr() ([]*ResourceType, error) {
	if e.loadedTypes[0] {
		return e.ResourceTypeFk, nil
	}
	return nil, &NotLoadedError{edge: "resource_type_fk"}
}

// PoliciesOrErr returns the Policies value or an error if the edge
// was not loaded in eager-loading.
func (e ResourceTypeClassEdges) PoliciesOrErr() ([]*PermissionsPolicy, error) {
	if e.loadedTypes[1] {
		return e.Policies, nil
	}
	return nil, &NotLoadedError{edge: "policies"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ResourceTypeClass) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullTime{},   // create_time
		&sql.NullTime{},   // update_time
		&sql.NullString{}, // name
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ResourceTypeClass fields.
func (rtc *ResourceTypeClass) assignValues(values ...interface{}) error {
	if m, n := len(values), len(resourcetypeclass.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	rtc.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullTime); !ok {
		return fmt.Errorf("unexpected type %T for field create_time", values[0])
	} else if value.Valid {
		rtc.CreateTime = value.Time
	}
	if value, ok := values[1].(*sql.NullTime); !ok {
		return fmt.Errorf("unexpected type %T for field update_time", values[1])
	} else if value.Valid {
		rtc.UpdateTime = value.Time
	}
	if value, ok := values[2].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field name", values[2])
	} else if value.Valid {
		rtc.Name = value.String
	}
	return nil
}

// QueryResourceTypeFk queries the resource_type_fk edge of the ResourceTypeClass.
func (rtc *ResourceTypeClass) QueryResourceTypeFk() *ResourceTypeQuery {
	return (&ResourceTypeClassClient{config: rtc.config}).QueryResourceTypeFk(rtc)
}

// QueryPolicies queries the policies edge of the ResourceTypeClass.
func (rtc *ResourceTypeClass) QueryPolicies() *PermissionsPolicyQuery {
	return (&ResourceTypeClassClient{config: rtc.config}).QueryPolicies(rtc)
}

// Update returns a builder for updating this ResourceTypeClass.
// Note that, you need to call ResourceTypeClass.Unwrap() before calling this method, if this ResourceTypeClass
// was returned from a transaction, and the transaction was committed or rolled back.
func (rtc *ResourceTypeClass) Update() *ResourceTypeClassUpdateOne {
	return (&ResourceTypeClassClient{config: rtc.config}).UpdateOne(rtc)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (rtc *ResourceTypeClass) Unwrap() *ResourceTypeClass {
	tx, ok := rtc.config.driver.(*txDriver)
	if !ok {
		panic("ent: ResourceTypeClass is not a transactional entity")
	}
	rtc.config.driver = tx.drv
	return rtc
}

// String implements the fmt.Stringer.
func (rtc *ResourceTypeClass) String() string {
	var builder strings.Builder
	builder.WriteString("ResourceTypeClass(")
	builder.WriteString(fmt.Sprintf("id=%v", rtc.ID))
	builder.WriteString(", create_time=")
	builder.WriteString(rtc.CreateTime.Format(time.ANSIC))
	builder.WriteString(", update_time=")
	builder.WriteString(rtc.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", name=")
	builder.WriteString(rtc.Name)
	builder.WriteByte(')')
	return builder.String()
}

// ResourceTypeClasses is a parsable slice of ResourceTypeClass.
type ResourceTypeClasses []*ResourceTypeClass

func (rtc ResourceTypeClasses) config(cfg config) {
	for _i := range rtc {
		rtc[_i].config = cfg
	}
}
