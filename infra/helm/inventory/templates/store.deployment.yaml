{{- /*
Copyright (c) 2004-present Facebook All rights reserved.
Use of this source code is governed by a BSD-style
license that can be found in the LICENSE file.
*/ -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "inventory.store.fullname" . }}
  labels: {{- include "inventory.store.metaLabels" . | nindent 4}}
  {{- with .Values.store.deploymentAnnotations }}
  annotations: {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.store.replicas }}
  selector:
    matchLabels: {{- include "inventory.store.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels: {{- include "inventory.store.selectorLabels" . | nindent 8 }}
      {{- with .Values.store.podAnnotations }}
      annotations: {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ template "inventory.store.serviceAccountName" . }}
      {{- with .Values.store.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.store.tolerations }}
      tolerations: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.store.affinity }}
      affinity: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- include "inventory.imagePullSecrets" . | indent 6 }}
      containers:
        - name: store
          image: {{ .Values.store.image.repository }}:{{ .Values.store.image.tag }}
          imagePullPolicy: {{ .Values.store.image.pullPolicy }}
          args:
            - --web.listen-address={{ printf ":%d" (int .Values.store.service.targetPort) }}
            - --bucket-url={{  required ".Values.store.spec.bucket.url is required" .Values.store.spec.bucket.url }}
          env:
            {{- include "inventory.loggingEnv" .Values.store | nindent 12 }}
            {{- include "inventory.tracingEnv" . | nindent 12 }}
          ports:
            - containerPort: {{ .Values.store.service.targetPort }}
              name: http
          {{- with .Values.store.probes.liveness }}
          livenessProbe:
            httpGet:
              path: /healthz/liveness
              port: http
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.store.probes.readiness }}
          readinessProbe:
            httpGet:
              path: /healthz/readiness
              port: http
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources: {{- toYaml .Values.store.resources | nindent 12 }}
