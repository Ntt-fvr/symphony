mutation addRule($input: AddRuleInput!) {
  addRule(input: $input) {
    id
    name
		gracePeriod
		ruleType {id}
		eventTypeName
		specificProblem
		additionalInfo
		status
		eventSeverity{id}
		threshold{id}

  }
}