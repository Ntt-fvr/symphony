mutation editRule($input: EditRuleInput!) {
  editRule(input: $input) {
    id
    name
	gracePeriod
	ruleType {id}
	eventTypeName
	specificProblem
	additionalInfo
	status
	eventSeverity{id}
	threshold{id}

  }
}