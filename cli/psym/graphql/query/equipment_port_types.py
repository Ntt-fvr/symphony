#!/usr/bin/env python3
# @generated AUTOGENERATED file. Do not Change!

from dataclasses import dataclass, field
from datetime import datetime
from gql_client.runtime.datetime_utils import DATETIME_FIELD_METADATA
from gql_client.runtime.variables import encode_variables
from gql import gql, Client
from gql.transport.exceptions import TransportQueryError
from functools import partial
from numbers import Number
from typing import Any, AsyncGenerator, Callable, Dict, List, Generator, Optional
from time import perf_counter
from dataclasses_json import DataClassJsonMixin

from ..fragment.equipment_port_type import EquipmentPortTypeFragment, QUERY as EquipmentPortTypeFragmentQuery

# fmt: off
QUERY: List[str] = EquipmentPortTypeFragmentQuery + ["""
query EquipmentPortTypesQuery {
  equipmentPortTypes {
    edges {
      node {
        ...EquipmentPortTypeFragment
      }
    }
  }
}

"""
]


class EquipmentPortTypesQuery:
    @dataclass(frozen=True)
    class EquipmentPortTypesQueryData(DataClassJsonMixin):
        @dataclass(frozen=True)
        class EquipmentPortTypeConnection(DataClassJsonMixin):
            @dataclass(frozen=True)
            class EquipmentPortTypeEdge(DataClassJsonMixin):
                @dataclass(frozen=True)
                class EquipmentPortType(EquipmentPortTypeFragment):
                    pass

                node: Optional[EquipmentPortType]

            edges: List[EquipmentPortTypeEdge]

        equipmentPortTypes: EquipmentPortTypeConnection

    # fmt: off
    @classmethod
    def execute(cls, client: Client) -> EquipmentPortTypesQueryData.EquipmentPortTypeConnection:
        variables: Dict[str, Any] = {}
        new_variables = encode_variables(variables)
        response_text = client.execute(
            gql("".join(set(QUERY))), variable_values=new_variables
        )
        res = cls.EquipmentPortTypesQueryData.from_dict(response_text)
        return res.equipmentPortTypes

    # fmt: off
    @classmethod
    async def execute_async(cls, client: Client) -> EquipmentPortTypesQueryData.EquipmentPortTypeConnection:
        variables: Dict[str, Any] = {}
        new_variables = encode_variables(variables)
        response_text = await client.execute_async(
            gql("".join(set(QUERY))), variable_values=new_variables
        )
        res = cls.EquipmentPortTypesQueryData.from_dict(response_text)
        return res.equipmentPortTypes
