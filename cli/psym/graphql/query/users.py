#!/usr/bin/env python3
# @generated AUTOGENERATED file. Do not Change!

from dataclasses import dataclass, field
from datetime import datetime
from gql_client.runtime.datetime_utils import DATETIME_FIELD_METADATA
from gql_client.runtime.variables import encode_variables
from gql import gql, Client
from gql.transport.exceptions import TransportQueryError
from functools import partial
from numbers import Number
from typing import Any, AsyncGenerator, Callable, Dict, List, Generator, Optional
from time import perf_counter
from dataclasses_json import DataClassJsonMixin

from ..fragment.user import UserFragment, QUERY as UserFragmentQuery

# fmt: off
QUERY: List[str] = UserFragmentQuery + ["""
query UsersQuery {
  users {
    edges {
      node {
        ...UserFragment
      }
    }
  }
}

"""
]


class UsersQuery:
    @dataclass(frozen=True)
    class UsersQueryData(DataClassJsonMixin):
        @dataclass(frozen=True)
        class UserConnection(DataClassJsonMixin):
            @dataclass(frozen=True)
            class UserEdge(DataClassJsonMixin):
                @dataclass(frozen=True)
                class User(UserFragment):
                    pass

                node: Optional[User]

            edges: List[UserEdge]

        users: Optional[UserConnection]

    # fmt: off
    @classmethod
    def execute(cls, client: Client) -> Optional[UsersQueryData.UserConnection]:
        variables: Dict[str, Any] = {}
        new_variables = encode_variables(variables)
        response_text = client.execute(
            gql("".join(set(QUERY))), variable_values=new_variables
        )
        res = cls.UsersQueryData.from_dict(response_text)
        return res.users

    # fmt: off
    @classmethod
    async def execute_async(cls, client: Client) -> Optional[UsersQueryData.UserConnection]:
        variables: Dict[str, Any] = {}
        new_variables = encode_variables(variables)
        response_text = await client.execute_async(
            gql("".join(set(QUERY))), variable_values=new_variables
        )
        res = cls.UsersQueryData.from_dict(response_text)
        return res.users
