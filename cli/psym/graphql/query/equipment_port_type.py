#!/usr/bin/env python3
# @generated AUTOGENERATED file. Do not Change!

from dataclasses import dataclass, field
from datetime import datetime
from gql_client.runtime.datetime_utils import DATETIME_FIELD_METADATA
from gql_client.runtime.variables import encode_variables
from gql import gql, Client
from gql.transport.exceptions import TransportQueryError
from functools import partial
from numbers import Number
from typing import Any, AsyncGenerator, Callable, Dict, List, Generator, Optional
from time import perf_counter
from dataclasses_json import DataClassJsonMixin

from ..fragment.equipment_port_type import EquipmentPortTypeFragment, QUERY as EquipmentPortTypeFragmentQuery

# fmt: off
QUERY: List[str] = EquipmentPortTypeFragmentQuery + ["""
query EquipmentPortTypeQuery($id: ID!) {
  port_type: node(id: $id) {
    ... on EquipmentPortType {
      ...EquipmentPortTypeFragment
    }
  }
}

"""
]


class EquipmentPortTypeQuery:
    @dataclass(frozen=True)
    class EquipmentPortTypeQueryData(DataClassJsonMixin):
        @dataclass(frozen=True)
        class Node(EquipmentPortTypeFragment):
            pass

        port_type: Optional[Node]

    # fmt: off
    @classmethod
    def execute(cls, client: Client, id: str) -> Optional[EquipmentPortTypeQueryData.Node]:
        variables: Dict[str, Any] = {"id": id}
        new_variables = encode_variables(variables)
        response_text = client.execute(
            gql("".join(set(QUERY))), variable_values=new_variables
        )
        res = cls.EquipmentPortTypeQueryData.from_dict(response_text)
        return res.port_type

    # fmt: off
    @classmethod
    async def execute_async(cls, client: Client, id: str) -> Optional[EquipmentPortTypeQueryData.Node]:
        variables: Dict[str, Any] = {"id": id}
        new_variables = encode_variables(variables)
        response_text = await client.execute_async(
            gql("".join(set(QUERY))), variable_values=new_variables
        )
        res = cls.EquipmentPortTypeQueryData.from_dict(response_text)
        return res.port_type
